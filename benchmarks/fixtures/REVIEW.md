# Fixture Corpus Review

This document serves as the formal sign-off for the 50+ golden tactical fixtures used for evaluating Scan64's evidence classification pipeline.

## Reviewer Sign-off

STATUS: PENDING — no independent human coach/reviewer has signed off yet. The
`expected_label` values in `golden_corpus.json` were authored during pipeline
implementation and have not been independently verified by a reviewer distinct
from the implementer, per the acceptance requirement ("every fixture has a
human-reviewed expected label distinct from the pipeline's own output").

This file is the tracked mechanism for that sign-off. Replace this block with
the reviewer's name, date, and confirmation once the independent review is
performed; do not treat the corpus as satisfying its acceptance criterion
until this section is updated.

- Signed off by: (pending)
- Date: (pending)
